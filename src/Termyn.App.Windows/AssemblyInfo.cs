using System.Runtime.CompilerServices;

// The keys each task action answers to, and how a shortcut is written down, are internal: the menu
// prints from the same table the outline matches against, and a test is what keeps the two honest.
[assembly: InternalsVisibleTo("Termyn.App.Windows.Tests")]
